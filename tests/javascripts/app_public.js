(function() {
  var expect, queuePosition;

  expect = require('chai').expect;

  queuePosition = require('../../public/javascripts/app').queuePosition;

  describe('window.onload', function() {
    return beforeEach(function() {
      var window;
      window = {
        onload: function() {}
      };
      return window.onload();
    });
  });

  describe;

}).call(this);
